import os
from marketplaces import Marketplace  # Assuming Marketplace is defined in this module

MARKETPLACE_CREDS = {
    # Amazon, eBay, Shopify, Walmart (as before)

    Marketplace.FACEBOOK: {
        "access_token": os.getenv("FACEBOOK_ACCESS_TOKEN"),
        "catalog_id": os.getenv("FACEBOOK_CATALOG_ID")
    },
    Marketplace.INSTAGRAM: {
        "access_token": os.getenv("INSTAGRAM_ACCESS_TOKEN"),
        "business_id": os.getenv("INSTAGRAM_BUSINESS_ID")
    },
    Marketplace.TIKTOK: {
        "app_key": os.getenv("TIKTOK_APP_KEY"),
        "app_secret": os.getenv("TIKTOK_APP_SECRET"),
        "access_token": os.getenv("TIKTOK_ACCESS_TOKEN"),
        "shop_id": os.getenv("TIKTOK_SHOP_ID")
    }
}